import PlaceholderPanel from '../../../shared/ui/PlaceholderPanel';

export default function MetadataView() {
  return <PlaceholderPanel title="MetadataView" body="Workspace per metadata model, regole e manifest." />;
}
