import PlaceholderPanel from '../../../shared/ui/PlaceholderPanel';

export default function SettingsView() {
  return <PlaceholderPanel title="SettingsView" body="Application and environment settings view." />;
}
